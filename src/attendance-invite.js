(()=>{'use strict';
const U='https://plzktdfumurucbkwdhbu.supabase.co',K='sb_publishable_FSfGEy-i8R8LdQ-5OJwYtQ_MI6AUvVN';
const client=window.supabase?.createClient(U,K,{auth:{persistSession:true}});
const e=x=>String(x??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const modal=h=>{document.body.insertAdjacentHTML('beforeend','<div class="modal-backdrop" id="inviteModal"><section class="modal">'+h+'</section></div>')};
window.openInviteUser=async()=>{const {data:{session}}=await client.auth.getSession();if(!session)return;modal('<button class="modal-close" onclick="document.getElementById(\'inviteModal\').remove()">×</button><div class="eyebrow">Access control</div><h2>Invite user</h2><p class="modal-sub">The invitation is sent by a server-side Edge Function so the service-role credential never reaches the browser.</p><form id="inviteForm"><div class="field"><label>Full name</label><input name="full_name" required></div><div class="field"><label>Email</label><input name="email" type="email" required></div><div class="field"><label>Role</label><select name="role"><option>teacher</option><option>viewer</option><option>admin</option></select></div><div id="inviteError"></div><div class="modal-footer"><button type="button" class="btn btn-secondary" onclick="document.getElementById(\'inviteModal\').remove()">Cancel</button><button class="btn btn-primary" id="inviteBtn">Send invitation</button></div></form>');document.getElementById('inviteForm').onsubmit=async ev=>{ev.preventDefault();const b=document.getElementById('inviteBtn');b.disabled=true;b.textContent='Sending…';const body=Object.fromEntries(new FormData(ev.target).entries());const {error}=await client.functions.invoke('invite-user',{body});if(error){document.getElementById('inviteError').innerHTML='<div class="error-box">'+e(error.message||'Invitation failed')+'</div>';b.disabled=false;b.textContent='Send invitation';return}document.getElementById('inviteModal').remove();alert('Invitation sent to '+body.email)}};
const old=window.openUserModal;window.openUserModal=(id)=>id?old(id):window.openInviteUser();
})();
