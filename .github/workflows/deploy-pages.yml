name: Deploy KITC Digital Management System to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate static site
        shell: python
        run: |
          from pathlib import Path
          import re

          html = Path("index.html").read_text(encoding="utf-8")
          refs = re.findall(r'(?:src|href)=["\']([^"\']+)["\']', html)
          local_refs = [r.split("?", 1)[0] for r in refs if not r.startswith(("http://", "https://", "#"))]
          missing = [r for r in local_refs if not Path(r).exists()]
          if missing:
              raise SystemExit("Missing local assets: " + ", ".join(missing))

          forbidden = ["src/secretary-editorial.css", "src/module-fix.js", "src/usb-bridge.js", "src/usb-data-sync.js", "src/usb-storage.js", "src/record-tools.js"]
          stale = [r for r in forbidden if r in html]
          if stale:
              raise SystemExit("Obsolete asset still referenced: " + ", ".join(stale))

          required = [
              "index.html",
              "src/styles.css",
              "src/usb-gate.css",
              "src/kitc-theme.css",
              "src/kitc-bootstrap.js",
              "src/main.js",
              "src/secretary-suite.js",
              "src/usb-gate.js",
              "src/documents.js",
              "src/members.js",
              "src/polish.js",
              "src/dashboard-polish.js",
              "src/runtime.js",
              "src/supabase-client.js",
              "supabase/schema.sql",
          ]
          missing_required = [r for r in required if not Path(r).exists()]
          if missing_required:
              raise SystemExit("Missing required project file(s): " + ", ".join(missing_required))

          if 'src/secretary-suite.js' not in html:
              raise SystemExit("Secretary suite must be loaded by index.html")
          if 'data-kitc-secretary-suite="true"' not in html:
              raise SystemExit("Secretary suite marker is missing")

          print("KITC static-site validation passed.")

      - name: Check JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          for file in src/*.js; do
            echo "Checking $file"
            node --check "$file"
          done

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
